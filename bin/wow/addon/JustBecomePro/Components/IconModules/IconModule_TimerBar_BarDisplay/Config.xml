<Ui>



	<Frame name="JustBecomePro_BarDisplayBarOptions" inherits="JustBecomePro_OptionsModuleContainer" virtual="true">
		<Size y="90"/>
		<Frames>
			<CheckButton parentKey="Invert" inherits="JustBecomePro_CheckTemplate">
				<Anchors>
					<!-- Left anchor is handled by DistributeFrameAnchorsLaterally in parent's OnLoad -->
					<Anchor point="TOP" y="-1"/>
				</Anchors>
				<Scripts>
					<OnLoad>
						JBP:CInit(self)

						self:SetTexts(JBP.L["ICONMENU_INVERTBARS"], JBP.L["ICONMENU_INVERTBARDISPLAYBAR_DESC"])
						self:SetSetting("BarDisplay_Invert")
					</OnLoad>
				</Scripts>
			</CheckButton>
			<Frame parentKey="FakeMax">
				<Size y="17"/>
				<Anchors>
					<Anchor point="TOP" y="-12"/>
					<Anchor point="RIGHT" x="-10"/>
				</Anchors>
				<Layers>
					<Layer level="ARTWORK">
						<FontString parentKey="text" inherits="GameFontHighlightSmall" justifyH="LEFT">
							<Anchors>
								<Anchor point="BOTTOMLEFT" relativePoint="TOPLEFT"/>
								<Anchor point="RIGHT"/>
							</Anchors>
						</FontString>
					</Layer>
				</Layers>
				<Frames>
					<EditBox parentKey="FakeMaxTime" inherits="JustBecomePro_TimeEditBoxTemplate" autoFocus="false" setAllPoints="true">
						<Scripts>
							<OnLoad>
								JBP:CInit(self)

								self:SetTexts(JBP.L["ICONMENU_FAKEMAX"], JBP.L["ICONMENU_FAKEMAX_DESC"])
								self:SetSetting("BarDisplay_FakeMax")

								self:CScriptAdd("ReloadRequested", function()
									self:SetShown(JBP.CI.icon.typeData.barIsTimer ~= false)
									local settings = self:GetSettingTable()
									self:SetEnabled(not settings.BarDisplay_Invert)
								end)
							</OnLoad>
						</Scripts>
					</EditBox>
					<EditBox parentKey="FakeMaxValue" inherits="JustBecomePro_InputBoxTemplate" autoFocus="false" setAllPoints="true">
						<Scripts>
							<OnLoad>
								JBP:CInit(self)

								self:SetTexts(JBP.L["ICONMENU_FAKEMAX"], JBP.L["ICONMENU_FAKEMAX_DESC"])
								self:SetSetting("BarDisplay_FakeMax")

								self:CScriptAdd("ReloadRequested", function()
									self:SetShown(JBP.CI.icon.typeData.barIsTimer == false)
									local settings = self:GetSettingTable()
									self:SetEnabled(not settings.BarDisplay_Invert)
								end)
							</OnLoad>
						</Scripts>
					</EditBox>
				</Frames>
				<Scripts>
					<OnLoad>
						self.text:SetText(JBP.L["ICONMENU_FAKEMAX"])
					</OnLoad>
				</Scripts>
			</Frame>

			<CheckButton parentKey="Reverse" inherits="JustBecomePro_CheckTemplate">
				<Anchors>
					<!-- Left anchor is handled by DistributeFrameAnchorsLaterally in parent's OnLoad -->
					<Anchor point="TOP" relativeKey="$parent.Invert" relativePoint="BOTTOM" y="3"/>
				</Anchors>
				<Scripts>
					<OnLoad>
						JBP:CInit(self)

						self:SetTexts(JBP.L["ICONMENU_REVERSEBARS"], JBP.L["ICONMENU_REVERSEBARS_DESC"])
						self:SetSetting("BarDisplay_Reverse")
					</OnLoad>
				</Scripts>
			</CheckButton>
			
			<CheckButton parentKey="BarGCD" inherits="JustBecomePro_CheckTemplate">
				<!-- Anchoring is handled by DistributeFrameAnchorsLaterally in parent's OnLoad -->
				<Scripts>
					<OnLoad>
						JBP:CInit(self)

						self:SetTexts(JBP.L["ICONMENU_ALLOWGCD"], JBP.L["ICONMENU_ALLOWGCD_DESC"])
						self:SetSetting("BarDisplay_BarGCD")
						
						self:CScriptAdd("ReloadRequested", function()
							self:SetShown(not JBP.CI.icon.typeData.hasNoGCD)
						end)
					</OnLoad>
				</Scripts>
			</CheckButton>
			
			
			<CheckButton parentKey="EnableColors" inherits="JustBecomePro_CheckTemplate">
				<Anchors>
					<!-- Left anchor is handled by DistributeFrameAnchorsLaterally in parent's OnLoad -->
					<Anchor point="TOP" relativeKey="$parent.Reverse" relativePoint="BOTTOM" y="3"/>
				</Anchors>
				<Scripts>
					<OnLoad>
						JBP:CInit(self)

						self:SetTexts(JBP.L["COLOR_OVERRIDE_GROUP"], JBP.L["COLOR_OVERRIDE_GROUP_DESC"])
						self:SetSetting("TimerBar_EnableColors")
					</OnLoad>
				</Scripts>
			</CheckButton>
			
			<CheckButton parentKey="ClassColor" inherits="JustBecomePro_CheckTemplate">
				<Anchors>
				</Anchors>
				<Scripts>
					<OnLoad>
						JBP:CInit(self)

						self:SetTexts(JBP.L["COLOR_USECLASS"], JBP.L["COLOR_USECLASS_DESC"])
						self:SetSetting("BarDisplay_ClassColor")
						
						self:CScriptAdd("ReloadRequested", function()
							local settings = self:GetSettingTable()
							self:SetShown(settings.TimerBar_EnableColors)
							local hasUnits = JBP.CI.icon.typeData.UsedProcessors[JBP.C.IconDataProcessor.ProcessorsByName.UNIT]
							self:SetEnabled(hasUnits)
							if not hasUnits then
								settings[self.setting] = false
							end

							self:GetParent():AdjustHeight()
						end)
					</OnLoad>
				</Scripts>
			</CheckButton>

			<Frame inherits="JustBecomePro_TimerBarColorsTemplate">
				<Anchors>
					<Anchor point="TOP" relativeKey="$parent.ClassColor" relativePoint="BOTTOM" y="-3" />
				</Anchors>
				<Scripts>
					<OnLoad inherit="prepend">
						self:CScriptAdd("ReloadRequested", function()
							local settings = self:GetSettingTable()
							self:SetShown(settings.TimerBar_EnableColors and not settings.BarDisplay_ClassColor)

							self:GetParent():AdjustHeight()
						end)
					</OnLoad> 
				</Scripts>
			</Frame>

		</Frames>
		<Scripts>
			<OnLoad>
				JBP:CInit(self)
				
				self:SetTitle(JBP.L["CONFIGPANEL_TIMERBAR_BARDISPLAY_HEADER"])
				
				self.Invert:ConstrainLabel(self.FakeMax)

				self.Reverse:ConstrainLabel(self.BarGCD)
				self.BarGCD:ConstrainLabel(self, "RIGHT")

				self.EnableColors:ConstrainLabel(self.ClassColor)
				self.ClassColor:ConstrainLabel(self, "RIGHT")

				JBP.IE:DistributeFrameAnchorsLaterally(self, 2, self.Invert, self.FakeMax)
				JBP.IE:DistributeFrameAnchorsLaterally(self, 2, self.Reverse, self.BarGCD)
				JBP.IE:DistributeFrameAnchorsLaterally(self, 2, self.EnableColors, self.ClassColor)
			</OnLoad>
		</Scripts>
	</Frame>
 
</Ui>